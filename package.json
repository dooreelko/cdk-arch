{
  "name": "cdk-arch-workspace",
  "private": true,
  "workspaces": [
    "packages/cdk-arch",
    "packages/example/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces && cd packages/c43 && cargo build",
    "clean": "npm run clean --workspaces --if-present",
    "e2e": "npm run e2e --workspaces --if-present",
    "test": "cd packages/cdk-arch && npm run test && cd ../c43 && cargo test"
  },
  "version": "0.1.0"
}
