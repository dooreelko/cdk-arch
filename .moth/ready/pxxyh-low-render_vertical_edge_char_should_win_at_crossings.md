Stage 5 paints edges in m.edges order, so at desperation-pass crossings the later-painted edge wins the cell — the design spec says the VERTICAL edge's char must be drawn over the horizontal one (per-cell segment-orientation priority). Crossings only occur in already-reported error states, so this is cosmetic, but it deviates from spec. Fix in _paint_edge (per-cell orientation priority) or persist pass-2 routing order on the model. Surfaced during Task 6 code review; deferred from the layout.py implementation (moth bzumk).
